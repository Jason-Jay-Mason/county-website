import { styled } from "@linaria/react";
import { theme } from "../styles/theme";

//#region styles
const div = {};
//#endregion

const LargeCtaButton = ({ children }) => {
  return <button>{children}</button>;
};

export default LargeCtaButton;
